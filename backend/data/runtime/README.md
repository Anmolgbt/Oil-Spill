# Runtime output

Empty by design. Results computed at run time — inference masks, derived source
regions, drift output — will be written here once real mode is connected.

Nothing in this folder is committed; treat it as disposable cache.
